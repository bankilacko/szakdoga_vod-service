apiVersion: v1
kind: Service
metadata:
  name: transcoding-service
  labels:
    app: transcoding-service
spec:
  selector:
    app: transcoding-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
